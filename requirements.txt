# ── Core API ─────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9
httpx>=0.27.0

# ── Classical ML (tabular — fraud, trust, anomaly) ───────────
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.0.0
imbalanced-learn>=0.12.0
joblib>=1.3.0

# ── Data ─────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
faker>=24.0.0
datasets>=2.18.0

# ── Image: Real CNN (ResNet-18) ───────────────────────────────
# CPU-only torch — installed via extra-index-url in Dockerfile
torch==2.2.2
torchvision==0.17.2
pillow>=10.2.0

# ── NLP: Real BERT (similarity + chat) ───────────────────────
sentence-transformers>=2.7.0
transformers>=4.40.0

# ── Viz ──────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

