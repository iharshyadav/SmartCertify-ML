# ── Core API ─────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
python-multipart>=0.0.9
httpx>=0.27.0

# ── Classical ML (tabular — fraud, trust, anomaly) ───────────
scikit-learn>=1.4.0
xgboost>=2.0.0,<3.0.0
lightgbm>=4.0.0
imbalanced-learn>=0.12.0
joblib>=1.3.0

# ── Data ─────────────────────────────────────────────────────
# numpy MUST be <2.0 — torch 2.2.2 was compiled against NumPy 1.x
numpy>=1.26.0,<2.0
pandas>=2.2.0
scipy>=1.13.0
faker>=24.0.0
datasets>=2.18.0

# ── Image: ResNet-18 CNN ──────────────────────────────────────
# CPU-only torch — installed via extra-index-url in Dockerfile
torch==2.2.2
torchvision==0.17.2
pillow>=10.2.0

# ── NLP ───────────────────────────────────────────────────────
# transformers 4.40.x is the last version that works with torch 2.2.2
# newer transformers requires torch>=2.4
transformers==4.40.2
sentence-transformers==2.7.0
tokenizers>=0.19.0,<0.20.0

# ── Viz ──────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0
